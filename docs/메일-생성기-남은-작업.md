# 메일 생성기 — 남은 작업 메모

작성: 2026-07-27 (갱신: 생성기 DB 전환·편집 화면 완료)
브랜치: `feat/mail-generator-json` (베이스: `feat/port-prototype-features`)

중단 시점 기준으로 무엇이 끝났고 무엇이 남았는지, 왜 그렇게 설계했는지를 적는다.
이어받는 사람이 이 문서만 읽고 작업을 재개할 수 있어야 한다.

---

## 지금 당장 해야 할 것

**Supabase SQL 두 개를 실행하지 않으면 기능이 동작하지 않는다.** DDL이라 앱의 anon 키로는
실행할 수 없고, 대시보드 SQL Editor에서 직접 돌려야 한다.

| 파일 | 없으면 생기는 일 | 상태 |
|---|---|---|
| `supabase/migration_mail_slot_name.sql` | 템플릿 이름 변경이 저장되지 않음 | 실행 완료 |
| `supabase/migration_mail_draft_logs.sql` | 저장 기록이 하나도 안 쌓이고 `/mail/log`가 안내 문구만 표시 | 실행 완료 |
| `supabase/migration_mail_generators.sql` | 생성기 목록이 DB 대신 코드 시드로만 뜨고 저장이 안 됨 | 실행 완료 |
| `supabase/migration_mail_generator_delete.sql` | 생성기 삭제가 막힘(앱이 사유를 명시해 실패시킴) | 실행 완료 |

실행하지 않아도 앱은 정상 동작한다. 실패는 전부 `.catch`로 삼켜서 메모리 상태로만 돈다.
다만 조용히 실패하므로, 기능이 안 먹으면 먼저 이 표를 확인할 것.

---

## 끝난 것

### 1. 템플릿 슬롯 가변화 (`4c2a0bb`, 베이스 브랜치)

슬롯 1~5 고정을 풀고 추가·이름변경·삭제가 가능해졌다.

핵심 결정은 슬롯 목록의 기준을 상수에서 **"행이 존재하면 템플릿이 존재한다"**로 옮긴 것이다.
상수와 DB를 합집합으로 두면 삭제한 슬롯이 새로고침에 되살아난다. 대신 빈 템플릿도 이름을
가지려면 행이 필요해서, 첫 진입 시 1~5를 시드로 만든다.

주의할 점 두 가지.

- `upsertSlot`은 `name`을 명시할 때만 이름 컬럼을 건드린다. 그렇지 않으면 300ms 자동 저장이
  매번 이름을 빈 문자열로 덮어쓴다.
- 빈 슬롯으로 전환할 때 베이스 템플릿을 채우는 판단은 행 존재가 아니라 **내용**(`isEmptySlot`)
  기준이다. 시드 행이 생긴 뒤에도 새 템플릿이 베이스 내용으로 열려야 한다.

### 2. 생성기 정의(JSON) 계약 (`c613e65`, `dc07930`)

생성기를 코드가 아니라 데이터로 추가하기 위한 스키마와 컴파일러.

- `lib/mail/generators/definition.ts` — zod 스키마. 이 파일이 계약 원본이다.
- `lib/mail/generators/compile.ts` — 정의 JSON을 기존 `GeneratorConfig`로 변환.
- `lib/mail/generators/seed.ts` — 기존 3종을 정의로 표현한 것.
- `lib/mail/generators/__tests__/verbatim.test.ts` — 이관 전후 동등성 검증.

설계 원칙은 세 가지다.

1. **필드 하나 = 플레이스홀더 하나.** 필드의 `name`이 곧 `{{...}}` 이름이고, 폼 표시명이
   다르면 `label`로 따로 준다(예: "도달"로 입력받아 `{{도달수}}`로 치환).
2. **계산식은 없다.** 기존 `computeValues`가 하던 일은 영문 state 키를 한글 플레이스홀더로
   옮기는 것과 날짜 표기를 바꾸는 것뿐이라, `date.format` 선택으로 전부 표현된다.
3. **한 필드가 여러 표기를 내야 하면 `also`에 이름을 명시한다.** 암묵 규칙을 두지 않는다.

`verbatim.test.ts`가 3종 모두에 대해 기본 제목·본문의 문자 단위 일치, 같은 입력에서의
`computeValues` 키·값 전체 일치, 렌더 결과 일치를 검증한다. **이 테스트는 반드시 통과시켜야
한다.** 기본 문구를 원본 그대로 유지하는 것이 PRD 전제였다.

이관하면서 확인한 사실도 적어 둔다.

- 안 쓰이는 플레이스홀더(`모집마감일전체`·`마감요일`·`디데이`·`직무명`·`활동분야`)는
  템플릿에서 사용 0회지만 **전부 보존한다.** 나중에 본문을 고칠 때 필요하다.
- 미선택 select 처리가 원래 서로 달랐다. sales만 빈 문자열, intern·activity는 첫 옵션.
  첫 옵션으로 통일했고, 폼이 항상 첫 옵션으로 시작하므로 실사용 결과는 같다.
- `josa`·`rawHtml`은 필드가 아니라 **출력 단위** 속성이다. 조사 처리는 "그 플레이스홀더가
  본문에서 조사와 붙는지"의 문제라 기본 출력과 부가 출력이 다를 수 있다.

### 3. 임시보관함 저장 기록 (`f23d223`)

Gmail 초안이 만들어질 때마다 한 줄 남기고 `/mail/log`에서 조회한다.

- `supabase/migration_mail_draft_logs.sql` — `mail_draft_logs` 테이블
- `lib/mail/draftLog.ts` — 기록·조회·필터(순수 함수)
- `app/mail/log/page.tsx` — 필터 화면
- `components/mail/GmailPanel.tsx` — 초안 저장 성공 직후 기록

필터는 생성기·템플릿·검색어(상대/받는사람/제목/템플릿)·기간을 지원한다. 행을 누르면 그때
입력했던 폼 값 전체를 펼쳐 본다.

설계상 짚어둘 점.

- **발송 기록이 아니라 저장 기록이다.** 이 앱은 메일을 보내지 않는다(`drafts.create`만).
- 슬롯이 지워져도 기록은 남아야 하므로 FK를 걸지 않고 그 시점의 템플릿 이름을 함께 박는다.
- `delete` 정책을 두지 않아 기록 삭제를 막는다.
- 로그 기록 실패가 초안 저장을 되돌리지 않는다. 초안은 이미 만들어진 뒤다.
- 날짜는 `localDateKey`로 로컬 기준으로 자른다. `toISOString`으로 자르면 한국 시간 오전 9시
  이전 기록이 전날로 밀린다.

어느 필드가 "상대 이름"인지는 `GeneratorConfig.counterpartyFieldId`가 지정한다. 코드 config
3종과 JSON 정의(`field.counterparty`) 양쪽에서 동일하게 동작하도록 심어 두었다.

---

### 4. 생성기 DB 전환 + 편집 화면

`/mail`의 탭 목록이 `mail_generators` 테이블에서 온다. 하드코딩 배열은 없앴다.

- `lib/mail/generators/store.ts` — 로드·시드·저장·감추기
- `app/mail/page.tsx` — DB 로드, 비었으면 시드, `compileGenerator`로 config 생성
- `app/mail/generators/page.tsx` — JSON 편집·검증·저장, 순서·감추기

동작 원칙 세 가지.

- **한 생성기가 깨져도 나머지는 뜬다.** 손으로 쓰는 JSON이라 언젠가 하나는 깨지는데, 그때
  `/mail` 전체가 백지가 되면 고칠 방법이 없어진다. `loadGenerators`가 개별 검증 후 `broken`
  으로 분리하고 화면에 경고로 띄운다.
- **DB에 닿지 못하면 코드 시드로 뜬다.** 내부 도구라 "DB가 안 되면 아무것도 못 쓴다"보다
  "저장은 안 되지만 메일은 만든다"가 낫다. 이때 경고를 띄워 저장이 안 되는 상태임을 알린다.
- **삭제 대신 감추기.** 슬롯·기본값·저장 기록이 generator key로 묶여 있어 삭제하면 되돌릴 수
  없다. `mail_generators`에 delete 정책을 두지 않았다.

저장 전 `parseGeneratorDefinition` 검증은 필수 경로다. 실패하면 저장을 막고 어디가 틀렸는지
경로와 함께 보여준다(`fields.1.name: 플레이스홀더 이름 "회사명"이(가) 중복됩니다` 형태).

폼 빌더 UI는 만들지 않았다. 같은 JSON 스키마 위에 나중에 얹을 수 있고, 지금은 구현 비용이
훨씬 크다.

---

## 남은 작업

### 1단계. 코드 config 정리

DB 정의가 원본이 되었으므로 `lib/mail/configs/{sales,intern,activity}.ts`의 config 객체는
역할이 끝났다. 다만 **기본 제목·본문 문구와 select 옵션의 원본이 아직 이 파일들**이고
`seed.ts`가 참조하고 있다. 시드는 테이블이 비었을 때만 도므로 평소에는 쓰이지 않지만,
DB 로드 실패 시 폴백으로도 쓰인다.

정리 순서:

1. 시드가 DB에 들어간 것을 확인
2. 문구·옵션을 `seed.ts`로 옮기거나, 시드 전용으로 남길지 결정
3. `verbatim.test.ts`는 비교 대상이 사라지므로 "현재 정의가 기대 문구와 같은지" 형태로 전환

이 단계는 급하지 않다. 두 벌이 공존하는 동안 `verbatim.test.ts`가 어긋남을 잡아 준다.

### 2단계. 생성기 정렬 순서 변경 UI

`sort_order`는 저장돼 있고 로드에도 반영되지만, 화면에서 바꿀 방법이 없다. 지금은 새 생성기가
항상 맨 뒤에 붙는다. 목록에서 위/아래로 옮기는 버튼 정도면 충분하다.

### 5. 생성기 삭제

목록의 삭제 버튼 → 확인 창에서 `삭제하겠습니다` 를 그대로 입력해야 실행된다.

창은 세 가지를 보여준다. **지워지는 것**(정의·템플릿 N개·기본값·입력값 — 개수는 삭제
직전에 세어서 채운다), **남는 것**(임시보관함 저장 기록 M건), **되살릴 생각이 있으면
감추기를 쓰라는 안내**.

남는 것을 함께 적는 이유는, 말하지 않으면 "다 지워졌겠지" 하고 나중에 로그에서 발견하고
당황하기 때문이다.

여기서 만난 함정을 적어 둔다. **RLS 는 권한이 없어도 에러를 내지 않고 0건 삭제로 끝난다.**
PostgREST 는 이를 성공(204)과 같은 응답으로 돌려주므로, 앱은 "지웠습니다" 라고 말하고
행은 그대로 남는다. 읽기는 빈 결과로 바로 티가 나지만 쓰기는 그렇지 않다. 그래서
`deleteGenerator` 는 지운 뒤 되읽어 확인하고, 남아 있으면 명시적으로 실패시킨다.

삭제 순서도 이 때문에 정했다. 정의를 먼저 지우고 **성공을 확인한 뒤에야** 딸린 템플릿·
기본값을 지운다. 반대로 하면 정의 삭제가 막혔을 때 데이터만 날아간 빈 껍데기가 남는다.

### 6. 알림·확인 창

`window.confirm`/`alert` 을 앱 안에서 그리는 창으로 모두 교체했다(메일 영역 8곳).

- `components/mail/dialog.tsx` — `alertDialog` / `confirmDialog` (Promise 반환) + `DialogHost`
- `app/mail/layout.tsx` — `/mail` 이하에 호스트를 하나 띄운다

컨텍스트가 아니라 모듈 스코프 스토어를 쓴다. `useMailGenerator` 같은 훅 안에서도 불러야 해서
프로바이더 트리에 의존하지 않는 편이 낫다. 화면 어딘가에 `DialogHost` 하나만 있으면 된다.

브라우저 기본 창을 버린 이유는 스타일 통일뿐이 아니다. 기본 창은 렌더링을 멈춰 세워
자동화·테스트 환경에서 세션을 막아 버린다.

`window.confirm` 이 동기였던 자리가 전부 `await` 로 바뀌었으므로, 교체할 때 해당 콜백을
`async` 로 바꿔야 한다(`saveSubjectDefault`, `removeSlot` 등).

`components/SalesApp.tsx` 의 확인 창은 메일 자동화가 아니라 기존 대시보드라 그대로 두었다.

---

## 알아둘 것

- **dev 서버를 켜 둔 채 `next build`를 돌리지 말 것.** 둘 다 같은 `.next`를 써서 dev 서버가
  참조하던 청크가 날아간다(`Cannot find module './331.js'`). 빌드 전에 dev를 내린다.
- **`.env`의 `APP_PASSWORD`를 바꾸면 dev 서버를 재시작해야 한다.** API 라우트(Node)와
  미들웨어(Edge)가 env를 따로 들고 있어서, `Reload env`만으로는 미들웨어가 옛 값을 유지한다.
  증상은 "로그인은 200인데 계속 `/login`으로 튕김"이다.
- **RLS 에서 쓰기 실패는 조용하다.** DELETE/UPDATE 는 권한이 없어도 0건 처리로 끝나 성공과
  같은 응답이 온다. 쓰기 뒤에는 결과를 되읽어 확인할 것.
- **ESLint는 이 프로젝트에 설정되어 있지 않다.** `npm run lint`는 초기 설정을 물어본다.
  검증은 `npx tsc --noEmit`와 `npx vitest run`으로 한다.
- 현재 테스트 233개. 브랜치를 정리하기 전에 전부 통과하는지 확인할 것.
